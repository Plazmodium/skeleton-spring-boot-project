package rest.skeleton.spring.boot.controller;

/**
 * Web layer (REST controllers).
 *
 * Responsibilities:
 * - Define HTTP endpoints (e.g., {@code /api/v1/sample-entities})
 * - Validate requests (@Valid) and map to service calls
 * - Return DTOs and standardized error responses (see {@code error} subpackage)
 * - Host documentation helpers like Swagger UI redirects
 */
