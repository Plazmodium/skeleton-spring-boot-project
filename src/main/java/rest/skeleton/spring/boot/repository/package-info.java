package rest.skeleton.spring.boot.repository;

/**
 * Persistence adapters (Spring Data JPA repositories).
 *
 * Responsibilities:
 * - Provide CRUD and pagination access to entities (e.g., {@code SampleEntityRepository})
 * - Avoid embedding business logic; prefer derived queries or specifications
 * - Serve the service layer; not exposed to controllers directly
 */
